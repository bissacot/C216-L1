# C216-L1

Repositório das atividades da disciplina C216 L1 - Sistemas Distribuídos.

## Backend

O backend foi desenvolvido utilizando FastAPI e possui configuração de ambiente com Poetry.

## Executando os testes

Os testes automatizados foram desenvolvidos utilizando Pytest.

### Instalar as dependências

Na raiz do projeto:

```bash
cd backend
poetry install --no-root